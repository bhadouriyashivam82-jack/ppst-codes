/*#include<iostream>    
#include<cmath>
using namespace std;

int main(){
    float a,b,c,d;
    float x1,x2;
    cout<<"enter the a,b,c for qudratic equation ";
    cin>>a>>b>>c;
    d=pow((b),2)-4*a*c;
    if (d==0)
    {
        x1=x2=(-b)/(2*a);
        cout<<x1<<x2;
    }
    else if (d>0)
    {
        x1=(-b+sqrt(d))/(2*a);
        x2=(-b-sqrt(d))/(2*a);
        cout<<x1;
        cout<<x2;
    }
    else
    {
         x1=(-b+sqrt(d))/(2*a);
        x2=(-b-sqrt(d))/(2*a);
   cout<<"i"<<x1;
   cout<<"i"<<x2;
    }

    return 0;
}*/
/*# include<iostream>
using namespace  std;
int main(){
    int a,b,c,d;
    cout<<"enter the any 4 intergers ";
   cin>>a;
    cin>>b;
    cin>>c;
    cin>>d;
    if (a>b) {
    if(a>c){ 
        if (a>d) {
        cout<<a;
        }
        else {
        cout<<d;
        }
    }
    }

    if(b>c){
    if(b>d){
        cout<<b;
    }
    else
    cout<<d;
}
   if(c>d){
    cout<<c;
   }
   else 
   cout<<d;
}*/


